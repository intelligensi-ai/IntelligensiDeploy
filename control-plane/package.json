{
  "name": "intelligensi-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic orchestration runtime for IntelligensiDeploy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}

